initSidebarItems({"struct":[["ShareableOrderedVec","A collection that keeps the ordering of its elements, even when deleting an element However, this collection can be shared between threads We can guess what the index is for an element that we must add We cannot get, get_mut, remove or push_shove on other threads, only guess the index We must do those operations using an external messaging system"],["ShareableOrderedVecState","A shareable state that can be created by a ShareableOrderedVec This helps since we cannot get, get_mut, remove or push_shove on other threads, so it makes it a bit safer"]]});